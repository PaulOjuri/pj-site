'use client'

interface RosieDialogProps {
  open?: boolean
  onClose?: () => void
}

export function RosieDialog({ open: _open, onClose: _onClose }: RosieDialogProps) {
  return null
}

interface RosieButtonProps {
  onClick?: () => void
}

export function RosieButton({ onClick: _onClick }: RosieButtonProps) {
  return null
}
