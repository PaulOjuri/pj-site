.venv/
__pycache__/
*.egg-info/
.pytest_cache/
# Never commit: engine binaries, puzzle DB, raw API caches
data/raw/
data/puzzles/
data/engines/
data/*.db-journal
data/*.db-wal
data/*.db-shm
# coach.db holds every PGN (~300MB+ with full online history) and exceeds GitHub's file limit.
# It is fully reproducible with `coach ingest`; analysis outputs will publish as JSON artifacts.
data/coach.db

# private analyses rendered locally only
data/private/
